{
    "build": {
      "builder": "NIXPACKS",
      "watchPatterns": ["food-recipe-recommender/*.py", "requirements.txt"]
    },
    "deploy": {
      "startCommand": "cd food-recipe-recommender && streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
    }
  }
  