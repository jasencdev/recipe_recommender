[project]
name = "recipe-recommender"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "starlette>=0.48.0",
    "uvicorn>=0.35.0",
]
